{{- if .Values.scheduler.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scheduler
  namespace: {{ include "buildbarn.namespace" . }}
  labels:
    {{- include "buildbarn.labels" . | nindent 4 }}
spec:
  {{- if .Values.scheduler.ingress.ingressClassName }}
  ingressClassName: {{ .Values.scheduler.ingress.ingressClassName }}
  {{- end }}
  rules:
  - host: {{ .Values.scheduler.ingress.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: scheduler
            port:
              name: http
{{- end }}
