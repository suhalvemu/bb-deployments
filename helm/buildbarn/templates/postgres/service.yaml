apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ include "buildbarn.namespace" . }}
  labels:
    {{- include "buildbarn.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
  - port: 5432
    protocol: TCP
    name: postgres
  selector:
    {{- include "buildbarn.selectorLabels" (dict "root" $ "component" "postgres") | nindent 4 }}
